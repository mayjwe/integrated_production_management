<Page x:Class="integrated_production_management.Pages.AddEditEquipment"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:integrated_production_management.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавить/Редактировать оборудование">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            <TextBlock Text="Инвентарный номер:"/>
            <TextBox Name="txtInventoryNumber" Margin="0,0,0,10"/>

            <TextBlock Text="Модель:"/>
            <TextBox Name="txtModel" Margin="0,0,0,10"/>

            <TextBlock Text="Тип оборудования:"/>
            <ComboBox Name="cbTypeEquipment" DisplayMemberPath="title" SelectedValuePath="id_type_equipment" Margin="0,0,0,10"/>

            <TextBlock Text="Дата ввода в эксплуатацию:"/>
            <DatePicker Name="dpDateInput" Margin="0,0,0,10"/>

            <TextBlock Text="Статус:"/>
            <ComboBox Name="cbStatus" DisplayMemberPath="title" SelectedValuePath="id_status_equipment" Margin="0,0,0,10"/>

            <TextBlock Text="График профилактики:"/>
            <ComboBox Name="cbSchedule" DisplayMemberPath="title" SelectedValuePath="id_schedule" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnSave" Content="Сохранить" Click="btnSave_Click" Width="100" Margin="0,0,5,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>